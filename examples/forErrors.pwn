arr: string[] = ["abc", "def", "ghi"];

for (b: string in a) {
    x1: int = 5;
}

for (i: bool in arr) {
    x2: int = 5;
}

for (arr: string in arr) {
    x3: int = 5;
}

not_array: bool = true;

for (element: bool in not_array) {
    x4: int = 5;
}

for (s: string in arr) {
    x5: int = 5;
}